<% layout('layouts/boilerplate') %> 
<h1 class="m-3">Ordenes de compra</h1>

<div class="container">
    <% for (purchase of compra) {%>
        <div class="row">
            <div class="col-9 order"><a class="btn btn-dark" href="/compras/show/<%=purchase._id%>"><%= purchase._id %></a> por <a class="btn btn-dark" href="/users/<%=purchase.author._id%>"><%= purchase.author.username %></a> </div>
        </div> 
    <% } %>  
</div>